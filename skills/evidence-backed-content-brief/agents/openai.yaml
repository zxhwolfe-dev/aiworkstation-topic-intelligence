interface:
  display_name: "Evidence-Backed Content Brief"
  short_description: "Turn a live Radar topic into a research-ready content brief"
  default_prompt: "Turn a current AI Workstation Global Topic Radar topic into one executable, evidence-aware content brief. Prefer a valid current-task Topic Opportunity handoff when available; otherwise resolve the topic live, or use the Skill's bounded one-topic selection fallback when this Skill is installed alone."
policy:
  allow_implicit_invocation: true
