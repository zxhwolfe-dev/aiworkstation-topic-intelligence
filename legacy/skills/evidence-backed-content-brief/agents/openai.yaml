interface:
  display_name: "Evidence-Backed Content Brief"
  short_description: "Turn a live Radar topic into a research-ready content brief"
  default_prompt: "Turn a current AI Workstation Global Topic Radar topic into one executable, evidence-aware content brief. Follow references/quality-contract.md: public Skill mode may use only feed/sources/history and must not call anonymous AI Workstation model-backed /insight; use the current host model for editorial reasoning, keep content-format constraints separate from Radar filters, preserve explicit topic scope, prefer a valid current-task Opportunity handoff without another selection pass, and keep Radar facts distinct from host analysis. Only use server Topic Insight when a native authenticated Premium AI Workstation connection explicitly provides it and enforces the user's quota."
policy:
  allow_implicit_invocation: true
