# AI Topic Intelligence Plugin Candidate

This directory is a prepared, validation-ready, skills-only Plugin candidate for a future directory submission. It contains no MCP server, App, authentication layer, or server-side model tool.

The bundled `topic-intelligence` Skill remains the source of runtime behavior. Update the canonical Skill first, then run:

```bash
python3 scripts/sync_plugin_candidate.py
python3 scripts/sync_plugin_candidate.py --check
python3 /home/zxhwolfe/.codex/auth-yinhe/skills/.system/plugin-creator/scripts/validate_plugin.py plugin-candidate/ai-topic-intelligence
```

## Current directory status

Public Plugin Directory submission is temporarily blocked by the OpenAI Platform payment-method and developer-identity verification prerequisite. This is an external distribution prerequisite, not a Topic Intelligence functional defect and not a v0.3.0 release blocker. It does not require v0.3.1.

Keep this candidate and its validated materials intact. Continue the public submission when the prerequisite is satisfied. The project does not claim that this Plugin is submitted to, approved by, or available in the public Plugin Directory.
