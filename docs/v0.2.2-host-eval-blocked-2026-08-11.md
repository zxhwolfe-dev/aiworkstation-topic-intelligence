# v0.2.2 Host Eval release blocker — 2026-08-11

Status: **BLOCKED — do not tag or publish v0.2.2**.

The live `v0.2.1` strict Host Eval collector was run with fresh Codex processes. The run produced 7 cases: 5 timeouts, 1 non-zero exit, and 2 unobservable route observations. The second-stage grader also initially treated `v0.2.1` as an unsupported quality suite and later proved too permissive about helper-path mentions; both tooling defects are now fixed. Regrading this run with the hardened rules produces 7 unobservable cases because it contains no successful contract-valid Radar helper response. This is an honest failed RC, not release evidence.

The raw and graded reports were inspected from the local run at:

- `/tmp/ati-v0.2.1-strict-20260811.json`
- `/tmp/ati-v0.2.1-evidence-20260811.json`

Those temporary reports are intentionally not copied into `release-evidence/` because they are incomplete and cannot satisfy the release gate. A future release candidate must rerun the suite successfully, persist both reports under `release-evidence/v<VERSION>/`, and add structured `manual-review.json` with the required per-case `must_show`/`must_not`, zero anonymous `/insight`, and no-handoff-reselection attestations. The executable check is `scripts/verify_release_evidence.py`.
