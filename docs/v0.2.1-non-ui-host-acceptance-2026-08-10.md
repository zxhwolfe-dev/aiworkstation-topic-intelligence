# v0.2.1 Non-UI Host Acceptance — 2026-08-10

This record closes the v0.2.1 host/runtime release gate without requiring a ChatGPT login.

## Classification

```text
NON_UI_HOST_ACCEPTANCE_PASS
```

Release-owner direction on 2026-08-10 explicitly selected non-UI testing instead of ChatGPT login/UI smoke. This record does not convert that choice into an unsupported ChatGPT product-surface claim.

## Evidence boundary

Validated here:

- the actual installed v0.2.1 RC Skill definitions and portable runtime files;
- isolated fresh-agent behavior for Creator-only, Brief-only, and both-Skills;
- live public `feed`, `sources`, and `history` access;
- exact topic identity across the composed handoff;
- public-mode completion by host reasoning with zero AI Workstation server-side LLM calls;
- helper and extracted-package behavior, deterministic archives, and blocked-live-data failure without local fallback.

Not validated here:

- uploading the v0.2.1 ZIP through the ChatGPT web UI;
- ChatGPT web discovery/scan behavior for the new ZIP;
- any hidden ChatGPT UI handoff trace.

The last manual ChatGPT web-upload evidence remains the immutable v0.2.0 record in `docs/chatgpt-v0.2.0-smoke-result-2026-08-09.md`.

## Candidate identity

- Validated source base: `7c0b91066d2ee623432693d503c773306f53f8d1`
- Version: `0.2.1`
- Installed Skill validation: both Skill directories passed `quick_validate.py`.
- Installed functional files (`SKILL.md`, helper, handoff contract, quality contract) byte-match the candidate source.
- Installed `agents/openai.yaml` has the same functional interface text plus installation-owned icon/product metadata.

## Fresh-agent results

Each run started without inherited project conclusions and was prohibited from editing the repository.

| Shape | Live workflow | Topic identity | Result |
|---|---|---|---|
| Creator-only | freshness/source coverage, bounded AI candidate discovery, finalist history | `topic:43863932371454d966d2`, `topic:6898d7d35bb54f9c4750`, `topic:5ffbe7ec365497e9ab19` | PASS |
| Brief-only | bounded AI selection, one finalist history, host-generated brief | `topic:43863932371454d966d2` | PASS |
| Both Skills | Creator selection → `ati.topic-opportunity-handoff.v1` → Brief | before and after handoff: `topic:43863932371454d966d2` | PASS |

Observed contract behavior:

- a stale initial Radar snapshot was refreshed and was not presented as current;
- the `AI` query's Fastmail/Taiwan/KaiCenat-style literal substring collisions were rejected as semantic AI candidates;
- format, duration, language, and audience were not mapped to Radar platform/source filters;
- Radar observations were separated from host editorial judgment;
- after the valid both-Skills handoff, Brief did not run another candidate-selection pass;
- all requested Brief sections were present, including audience payoff, angle, opening, narrative, `must_verify`, `avoid_claims`, research handoff, and visual/material needs;
- no run called `/insight`, requested a shared credential, or consumed an AI Workstation server-side LLM.

## Live Gate B

Final live snapshot used by the acceptance:

- generated at `2026-08-10T08:13:24Z`;
- `stale=false`;
- `partial=false`;
- `refreshing=false`;
- `history_available=true`;
- 37 sources: 34 `healthy`, 3 `empty`, and no failed source;
- 737-topic pool, with Top-24 primary-source ratio `0.708` and coverage quota `met`.

The selected topic history request succeeded for `topic:43863932371454d966d2`. A forced request to `http://127.0.0.1:9` failed closed with `Connection refused`, exit code 2, and no local/cached fallback output.

## Offline and package gates

- runtime sync: PASS;
- unit tests before release finalization: 104/104 PASS;
- all three helper CLIs expose only `feed`, `sources`, and `history`;
- public helper operations are GET-only and contain no shared credentials;
- two independent release builds produced identical `SHA256SUMS`;
- extracted standalone runtime execution is covered by the passing release E2E suite.

Expected deterministic artifacts:

```text
3381d798c29cc8f67b1bca3f1f6da8a34a34ab78e64b6af7bd48aff95b663bb6  aiworkstation-topic-intelligence-0.2.1-creator-topic-opportunity-research.zip
81d6aac45b42c27b8f24c27ac18b6a268509fb9a8a88a0813b96feab8f034d5e  aiworkstation-topic-intelligence-0.2.1-evidence-backed-content-brief.zip
```

## Decision

The non-UI host/runtime gate is green. v0.2.1 may be released provided the release-prep diff remains documentation/test-only, CI passes on the release commit, the tag matches `VERSION`, and the published assets match the hashes above.
