# AI Workstation Topic Intelligence v0.3.0

AI Workstation Topic Intelligence is one public Skill for creators, researchers, and editors. It reads current public signals from the AI Workstation Global Topic Radar and uses the user's current host model to support three workflows:

1. shortlist current topics worth researching;
2. turn a supplied current Radar topic into a research-ready content brief;
3. select one current topic and continue directly into a brief for the same finalist.

## Download

Download **`topic-intelligence-0.3.0.zip`** from the assets below. The archive contains the complete `topic-intelligence` Skill.

SHA256:

```text
935bab465811a3efabd50ee46c3166c702ad719d19fd66ade718d871b69b066e
```

The public Skill does not require an AI Workstation API key and does not consume AI Workstation server-side LLM quota. Radar provides public evidence; the user's current ChatGPT, Codex, or other compatible host model performs the editorial reasoning.

## Install

- **Codex:** install the ZIP as the `topic-intelligence` Skill, or use the repository installer documented in the README.
- **ChatGPT:** use a workspace that supports Personal Skills and upload the ZIP. Product availability depends on the current workspace and plan. v0.3.0 has strict Codex Host Eval evidence, but has not yet been re-uploaded and manually validated in the ChatGPT Web UI.

## Try it

Topic shortlist:

```text
Which AI topics are worth researching or covering today? Check Radar freshness first, then give me the three strongest candidates. Do not write a full brief.
```

Supplied topic to brief:

```text
Use this current Radar card and exact topic ID to create a research-ready content brief. Only query history if movement evidence is genuinely needed, and do not select a different topic.
```

Selection followed by brief:

```text
Choose one current AI topic worth researching, then create a research-ready brief for that same topic. Use one bounded scan and preserve the exact Radar topic ID.
```

## Evidence and limitations

- Radar facts, host editorial judgment, and unknowns remain visibly separate.
- Radar does not measure actual audience size, topic saturation, or future virality.
- `stale` or `partial` responses must be disclosed.
- The public helper exposes only read-only `feed`, `sources`, and `history` operations. It does not expose anonymous server-side `/insight` model execution.
- v0.2.0 remains the latest version manually validated through the ChatGPT Web ZIP upload flow. The v0.3.0 release is validated by a complete seven-case live Codex Host Eval, semantic grading, manual review, and persistent release evidence.

Documentation: [English README](../../README.md) | [中文 README](../../README.zh-CN.md) | [Installation](../chatgpt-install.md)
